%section.volume
  %aside.volume-image
    = image_tag "volume#{volume.number}-500w.jpg"
  %article.volume-content
    %h4= data.volume_titles["volume#{volume.number}"]
    %p= volume.content
    %ul
      - volume.lessons.each do |key, lesson|
        %li= link_to "#{data.lesson_titles[lesson]}", "/lessons/#{lesson}"
