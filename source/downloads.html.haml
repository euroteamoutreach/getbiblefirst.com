---
title: Downloads
---

.page-content
  %h1 Downloads

  %p
    The following are free PDF and Word files which you may download and print
    as you manage your Bible First course.

  .download-segment
    %h4 Complete Downloads Package
    %p
      If you prefer, you can download every file listed on this page as a single
      zip file. This package includes all Q&ampA booklets, answer keys,
      accessory documents, and certificates.
    %ul.fa-ul
      %li
        %i.fa-li.fa.fa-file-pdf-o
        = link_to "bf_downloads_complete.zip (17 MB)",
          "/files/bf_downloads_complete.zip"

  .download-segment
    %h4 Q&amp;A Booklets
    %ul.fa-ul
      - data.files.qa_booklets.each do |booklet|
        = partial "partials/qa-booklet", locals: { booklet: booklet }
    = link_to("#", class: "back-to-top") do
      %i.fa.fa-sort-asc{ "aria-hidden" => "true" }
      top

  .download-segment
    %h4 Answer Keys
    %ul.fa-ul
      - data.files.answer_keys.each do |booklet|
        = partial "partials/answer-key", locals: { booklet: booklet }
    = link_to("#", class: "back-to-top") do
      %i.fa.fa-sort-asc{ "aria-hidden" => "true" }
      top

  .download-segment
    %h4 Accessory Documents
    %ul.fa-ul
      - data.files.accessory_docs.each do |doc|
        = partial "partials/accessory-doc", locals: { doc: doc }
    = link_to("#", class: "back-to-top") do
      %i.fa.fa-sort-asc{ "aria-hidden" => "true" }
      top

  .download-segment
    %h4 Certificates
    %p
      NOTE: In order for the editable certifcates below to display properly
      when opened in Microsoft Word, you will need to download and install the
      = link_to "CyrillicGoth", "/files/CyrillicGoth.ttf"
      font.
    %ul.fa-ul
      - data.files.certificates.each do |cert|
        = partial "partials/certificate", locals: { cert: cert }
    = link_to("#", class: "back-to-top") do
      %i.fa.fa-sort-asc{ "aria-hidden" => "true" }
      top
